package cambridge.structures;

import cambridge.io.streams.MemoryInputStream;

public class LevelKey extends GameObject
{
    @Override
    public void load(MemoryInputStream stream)
    {
        super.load(stream);
    }
}
