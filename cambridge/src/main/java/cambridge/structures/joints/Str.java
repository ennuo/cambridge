package cambridge.structures.joints;

import cambridge.io.streams.MemoryInputStream;
import cambridge.structures.Joint;

public class Str extends Joint
{
    @Override
    public void load(MemoryInputStream stream)
    {
        super.load(stream);
    }
}
